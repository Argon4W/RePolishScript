stdlib = import "./rps_stdlib.rps";

("Hello %s %s %s%n", ["RePolish", "Script", 1024]) -> $stdlib.writeFormat;

input = () -> $stdlib.readLine;

result = if ($input == "hello") {
    return "Input is hello";
} else {
    return "Input is not hello";
};

$result -> $stdlib.writeLine;